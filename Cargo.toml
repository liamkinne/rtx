[workspace]
resolver = "2"
members = ["driver", "line-reader"]

[workspace.dependencies]
line-reader = { path = "line-reader" }

heapless = { version = "0.9.3", features = ["defmt"] }
defmt = "1.1.0"
